# EcoHaul Week 1 Frontend

## Install

```powershell
cd frontend
npm install
```

## Run

```powershell
npm run dev
```

## Build check

```powershell
npm run build
```

Set `VITE_API_URL` in `.env` if backend is not on `http://localhost:8000`.
